let https = require('https');
let mongoose = require('mongoose');

export.getBiWeekly = function(request, response) {
};

export.getMonthly = function(request, response) {
};

export.updateDay = function(request, response) {
};
